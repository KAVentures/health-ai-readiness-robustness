# Copy to .env and fill in. NEVER commit the real .env (it is gitignored).
# Keys are read from the environment; none are stored in this repository.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
XAI_API_KEY=
GOOGLE_API_KEY=

# Optional, depending on how you route inference (see medmarks docs):
# PRIME_API_KEY=
# PRIME_TEAM_ID=
# MEDARC_INCLUDE_USAGE=false
